## 清理范围与原则
- 只清理仓库内自研/临时测试脚本与孤立测试文件，绝不改动 `node_modules`。
- 保留被 `package.json` 脚本引用的测试文件：`test-device-monitor.js`、`test-adb.js`（参见 `package.json:20-21`）。
- 删除未被任何地方引用的演示/验证脚本以及孤立的 `*.test.ts`。

## 拟删除文件清单（项目内）
- `src/utils/transferSystem.test.ts`
- 根目录未引用的演示/验证脚本：
  - `test-monitor-fix.js`
  - `test-quick.js`
  - `test-device-disconnection.js`
  - `test-direct-android.js`
  - `test-android.js`
  - `test-tool-paths.js`
  - `test-complete-transfer.cjs`
  - `test-path-config-simple.js`
  - `test-transfer-paths.js`
  - `test-transfer-paths.mjs`
  - `test-chinese-encoding.js`
  - `test-push.js`
- `scripts/test-path-config.js`

## 保留文件（因脚本引用）
- `test-device-monitor.js`（由 `npm run test:monitor` 调用，`package.json:20`）
- `test-adb.js`（由 `npm run test:adb` 调用，`package.json:21`）

## 具体操作步骤
1. 删除“拟删除文件清单”中的所有文件。
2. 全仓库检索确认无任何残留引用（导入、路径字符串、`require`）。
3. 运行 `npm run dev` 验证开发环境正常；如需验证保留的测试脚本，运行：
   - `npm run test:monitor`
   - `npm run test:adb`
4. 运行 `npm run build` 验证构建流程无误。

## 验证与回滚
- 验证：确保开发/构建/保留测试脚本均可正常运行，无报错或缺失文件提示。
- 回滚：若发现误删，恢复对应文件或（如不再需要）同步清理脚本引用。

## 后续建议（可选）
- 若以后需要保留自动化测试，统一采用单一测试框架（如 Vitest/Jest），并将临时脚本的关键用例迁移为标准测试用例，避免散落的脚本难以维护。